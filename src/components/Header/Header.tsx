import {FC} from "react";
import {Typography} from "@mui/material";

const Header:FC = () => {
    return(
        <header>
            <Typography variant='h3'>
                Todo List App
            </Typography>
        </header>
    )
}

export default Header